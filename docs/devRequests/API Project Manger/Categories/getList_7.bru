meta {
  name: getList_7
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/categories
  body: none
  auth: bearer
}

params:query {
  ~sort: 
  ~range: 
  ~filter: 
}

auth:bearer {
  token: {{token}}
}
