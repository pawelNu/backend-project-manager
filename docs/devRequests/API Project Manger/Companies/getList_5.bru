meta {
  name: getList_5
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/companies
  body: none
  auth: bearer
}

params:query {
  ~sort: 
  ~range: 
  ~filter: 
}

auth:bearer {
  token: {{token}}
}
