meta {
  name: getList
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/projects
  body: none
  auth: bearer
}

params:query {
  ~sort: 
  ~range: 
  ~filter: 
}

auth:bearer {
  token: {{token}}
}
