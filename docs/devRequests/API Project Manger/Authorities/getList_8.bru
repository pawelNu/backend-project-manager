meta {
  name: getList_8
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/authorities
  body: none
  auth: bearer
}

params:query {
  ~sort: 
  ~range: 
  ~filter: 
}

auth:bearer {
  token: {{token}}
}
