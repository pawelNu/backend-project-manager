meta {
  name: create_8
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/authorities
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "nameBackend": "",
    "nameFrontend": ""
  }
}
