get:
  tags:
    - CompaniesController
  operationId: getAllCompanies
  parameters:
    - name: pageNumber
      in: query
      required: true
      schema:
        type: integer
        format: int32
    - name: pageSize
      in: query
      required: true
      schema:
        type: integer
        format: int32
    - name: sortingField
      in: query
      required: true
      schema:
        type: string
    - name: isAscendingSorting
      in: query
      required: true
      schema:
        type: boolean
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: ../components/schemas/companies/CompanyListResponseDTO.yaml
